<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="de.pb92.snapshotmeter.MeterReadingOverview" >

    <ListView
        android:id="@+id/listView1"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentTop="true"
        android:layout_above="@+id/button2"
        android:layout_centerHorizontal="true" >
    </ListView>

    <Button
        android:id="@+id/deleteButton"
        android:layout_width="140dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="14dp"
        android:background="@color/red"
        android:onClick="deleteMeter"
        android:text="@string/meter_reading_overview_delete" />

    <Button
        android:id="@+id/button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/deleteButton"
        android:layout_centerHorizontal="true"
        android:layout_alignLeft="@+id/deleteButton"
        android:layout_alignRight="@+id/deleteButton"
        android:onClick="goBack"
        android:text="@string/meter_reading_overview_back" />

</RelativeLayout>
